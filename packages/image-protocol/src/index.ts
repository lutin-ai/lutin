// Postcard bindings for the image workflow's plugin protocol. Shared
// between the image iframe (`workflows/image/ui`) and any other host
// component that decodes its responses. Authoritative source is the
// Rust types in `workflows/image/src/lib.rs`.

export * from "./image";
